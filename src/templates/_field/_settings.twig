{#
/**
 * Easy Address Field plugin for Craft CMS
 *
 * EasyAddressFieldFieldType HTML
 *
 * @author    Studio Espresso
 * @copyright Copyright (c) 2017 Studio Espresso
 * @link      https://studioespresso.co
 * @package   EasyAddressField
 * @since     1.0.0-alpha
 */
#}

{% import "_includes/forms" as forms %}

<div class="field">
    <div class="heading">
        <table class="editable">
            <thead>
                <tr>
                    <th class="header" scope="col">{{ "Field"|t('easy-address-field') }}</th>
                    <th class="header" scope="col">{{ "Enable"|t('easy-address-field') }}</th>
                </tr>
            </thead>
            <tbody class="layout-table-rows">
                <tr>
                    <td class="layout-table-label"> <label for="enabledFields[name]"> {{ 'Name'|t('easy-address-field') }}</label></td>
                    <td class="layout-table-enable"> <input type="checkbox" id="enabledFields[name]" name="enabledFields[name]" value="name" {% if field.enabledFields.name is defined %}checked{% endif %}/></td>
                </tr>
                <tr>
                    <td class="layout-table-label"> <label for="enabledFields[street2]"> {{ 'Street 2'|t('easy-address-field') }}</label></td>
                    <td class="layout-table-enable"> <input type="checkbox" id="enabledFields[street2]" name="enabledFields[street2]" value="street2" {% if field.enabledFields.street2 is defined %}checked{% endif %}/></td>
                </tr>
                <tr>
                    <td class="layout-table-label"> <label for="enabledFields[state]"> {{ 'State'|t('easy-address-field') }}</label></td>
                    <td class="layout-table-enable"> <input type="checkbox" id="enabledFields[state]" name="enabledFields[state]" value="state" {% if field.enabledFields.state is defined %}checked{% endif %}/></td>
                </tr>
                <tr>
                    <td class="layout-table-label"> <label for="enabledFields[country]"> {{ 'Country'|t('easy-address-field') }}</label></td>
                    <td class="layout-table-enable"> <input type="checkbox" id="enabledFields[country]" name="enabledFields[country]" value="country" {% if field.enabledFields.country is defined %}checked{% endif %}/></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

        {{ forms.selectField({
            label:      "Default country"|t('easy-address-field'),
            id:         "defaultCountry",
            name:       "defaultCountry",
            options:    countries,
            value:      field.defaultCountry|length ? field.defaultCountry : 'US'
        }) }}

        {{ forms.lightswitchField({
            label:        "Get coordinates for addresses"|t('easy-address-field'),
            id:           'geoCode',
            name:         'geoCode',
            on:           field.geoCode,
        }) }}

        {{ forms.lightswitchField({
            label:        "Show coordinates?"|t('easy-address-field'),
            id:           'showCoordinates',
            name:         'showCoordinates',
            on:           field.showCoordinates,
        }) }}
    </div>
</div>